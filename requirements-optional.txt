# Optional: semantic ingredient matching (nourish/rag.py).
# Heavy — pulls torch (~2GB). Only needed to enable embeddings-based
# resolution as a fallback when exact/alias/fuzzy matching is weak.
# Install with:  pip install -r requirements-optional.txt
sentence-transformers>=2.2
chromadb>=0.4

# Optional: Layer 2 of the safety input rail (nourish/agent/safety.py) — a NeMo
# Guardrails self-check that catches subtly phrased self-harm intent the
# built-in patterns miss. The deterministic Layer 1 protects the app without
# it, so this is optional; disable at runtime with NOURISH_SAFETY_NEMO=0.
nemoguardrails>=0.11
